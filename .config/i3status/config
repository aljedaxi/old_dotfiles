general {
	output_format = i3bar
	separator = |
	colors = true
	interval = 30
}

#order += "run_watch DHCP"
#order += "battery all"
#order += "wireless wlan0"
#order += "ethernet eth0"
#order += "disk /"
#order += "cpu_usage"
order += "tztime local"

tztime local {
   format = "%m月%d日 %H:%M:%S"
}
